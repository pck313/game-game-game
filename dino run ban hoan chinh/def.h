#ifndef _DEFS__H
#define _DEFS__H

#include <SDL.h>
#include <SDL_image.h>

extern int width;
extern int height;
extern int char* title;

extern SDL_Window* window;
extern SDL_Renderer* renderer;

#endif
